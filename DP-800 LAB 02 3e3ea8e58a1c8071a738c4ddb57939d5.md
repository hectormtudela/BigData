# DP-800 | LAB 02

## Fase 0: Preparación del entorno (restauración de AdventureWorksLT)

**Entorno usado:** SQL Server local (Developer)

### Paso A — Identificar versión de SQL Server

```sql
SELECT @@VERSION;
```

Esto determina qué versión del archivo `.bak` descargar (ej. `AdventureWorksLT2019.bak`, `AdventureWorksLT2022.bak`).

### Paso B — Descargar el backup

Página oficial de Microsoft:
👉 https://learn.microsoft.com/en-us/sql/samples/adventureworks-install-configure

En la sección **Download backup files**, columna **Lightweight**, descargar el `.bak` correspondiente a la versión detectada en el Paso A.

### Paso C — Localizar la carpeta de backup de la instancia

```sql
SELECT SERVERPROPERTY('InstanceDefaultBackupPath') AS BackupPath;
```

### Paso D — Mover el archivo `.bak`

Copiar manualmente el `.bak` descargado a la ruta obtenida en el Paso C.

### Paso E-I — Restaurar la base de datos vía SSMS

1. Object Explorer → clic derecho en **Databases** → **Restore Database...**
2. Seleccionar **Device** → clic en **(...)**
3. **Add** → seleccionar el archivo `.bak` → **OK**
4. Revisar pestaña **Files** (rutas de restauración por defecto)
5. **OK** para ejecutar la restauración
6. Verificar en Object Explorer (Refresh) que aparece **AdventureWorksLT**

---

## Fase 1: Verificar conexión y datos de origen

```sql
-- Verificar tablas clave en AdventureWorksLT
SELECT TOP (5) CustomerID, FirstName, LastName
FROM SalesLT.Customer;

SELECT TOP (5) SalesOrderID, OrderDate, CustomerID
FROM SalesLT.SalesOrderHeader;

SELECT TOP (5) ProductID, Name, ListPrice
FROM SalesLT.Product;
```

**Resultado esperado:** tres conjuntos de resultados (clientes, pedidos, productos), hasta 5 filas cada uno.

![image.png](image.png)

---

## 

## Fase 2: Crear una vista (`SalesLT.vCustomerOrders`)

```sql
CREATE OR ALTER VIEW SalesLT.vCustomerOrders AS
SELECT    c.CustomerID,
    CONCAT(c.FirstName, ' ', c.LastName) AS CustomerName,
    h.SalesOrderID,
    h.OrderDateFROM SalesLT.Customer c
INNER JOIN SalesLT.SalesOrderHeader h ON c.CustomerID = h.Custome
```

**Validación:**

```sql
SELECT TOP (5) *
FROM SalesLT.vCustomerOrders
ORDER BY OrderDate DESC;
```

![image.png](image%201.png)

![image.png](image%202.png)

## Fase 3: Crear el procedimiento almacenado (`dbo.AddOrderLineItem`)

**Paso 1** — Crea el procedimiento:

```sql
CREATE OR ALTER PROCEDURE dbo.AddOrderLineItem	
@SalesOrderID INT,	
@ProductID    INT,	
@Quantity     INT
AS
BEGIN	
	SET NOCOUNT ON;	
	BEGIN TRANSACTION;
	-- Use Product ListPrice as UnitPrice	
	DECLARE @UnitPrice DECIMAL(18,2);	
	SELECT @UnitPrice = CAST(ListPrice AS DECIMAL(18,2))	
	FROM SalesLT.Product	
	WHERE ProductID = @ProductID;	
	
	IF @UnitPrice IS NULL	
	BEGIN		
		ROLLBACK TRANSACTION;
		THROW 50010, 'Invalid ProductID specified.', 1;	
		END
		-- Ensure SalesOrderID exists	
		IF NOT EXISTS (SELECT 1 FROM SalesLT.SalesOrderHeader WHERE SalesOrderID = @SalesOrderID)	
		BEGIN
				ROLLBACK TRANSACTION;		
				THROW 50011, 'Invalid SalesOrderID specified.', 1;	
		END
		-- Insert line item (no discount)	
		INSERT INTO SalesLT.SalesOrderDetail (SalesOrderID, OrderQty, ProductID, UnitPrice, UnitPriceDiscount)	
		VALUES (@SalesOrderID, @Quantity, @ProductID, @UnitPrice, 0);
		
		-- Update header subtotal based on current line totals	
		UPDATE h	
		SET SubTotal = d.SumLineTotal,		
				ModifiedDate = SYSUTCDATETIME()	
		FROM SalesLT.SalesOrderHeader h	
		INNER JOIN (
				SELECT SalesOrderID, SUM(LineTotal) AS SumLineTotal		
				FROM SalesLT.SalesOrderDetail		
				WHERE SalesOrderID = @SalesOrderID		
				GROUP BY SalesOrderID	
				) d ON d.SalesOrderID = h.SalesOrderID;	
				
				COMMIT TRANSACTION;
				
				END;
```

**Prueba:**

```sql
DECLARE @SalesOrderID INT = (SELECT TOP 1 SalesOrderID                            
														FROM SalesLT.SalesOrderHeader                            
														ORDER BY SalesOrderID DESC);
EXEC dbo.AddOrderLineItem @SalesOrderID = @SalesOrderID,                            
													@ProductID = 680,
													@Quantity = 1;
SELECT TOP (5) *
FROM SalesLT.SalesOrderDetail
WHERE SalesOrderID = @SalesOrderID
ORDER BY SalesOrderDetailID DESC;

SELECT SalesOrderID, SubTotal, TaxAmt, Freight, TotalDue
FROM SalesLT.SalesOrderHeader
WHERE SalesOrderID = @SalesOrderID;
```

## Fase 4: Crear la función escalar (`dbo.fnOrderTotal`)

Esta función devuelve el total de un pedido sumando las líneas de `SalesOrderDetail`.

```sql
CREATE OR ALTER FUNCTION dbo.fnOrderTotal (@OrderID INT)
RETURNS DECIMAL(18,2)
AS
BEGIN	
	DECLARE @Total DECIMAL(18,2);	
	SELECT @Total = SUM(LineTotal)	
	FROM SalesLT.SalesOrderDetail	
	WHERE SalesOrderID = @OrderID;	
	RETURN ISNULL(@Total, 0.00);
END;
```

**Uso:**

```sql
SELECT d.SalesOrderID, dbo.fnOrderTotal(d.SalesOrderID) AS OrderTotal
FROM SalesLT.SalesOrderDetail d
GROUP BY d.SalesOrderID
ORDER BY d.SalesOrderID DESC;
```

![image.png](image%203.png)

![image.png](image%204.png)

## Fase 5: Crear la función de tabla en línea (`dbo.GetCustomerOrders`)

```sql
CREATE OR ALTER FUNCTION dbo.GetCustomerOrders (@CustomerID INT)
RETURNS TABLE
AS
RETURN
(	
SELECT		
		h.SalesOrderID,
		h.OrderDate	
FROM SalesLT.SalesOrderHeader h	
WHERE h.CustomerID = @CustomerID
);
```

**Consulta directa:**

```sql
SELECT *
FROM dbo.GetCustomerOrders(29929)
ORDER BY OrderDate DESC;
```

**Unión con clientes (CROSS APPLY):**

```sql
SELECT CONCAT(c.FirstName, ' ', c.LastName) AS CustomerName, o.SalesOrderID, o.OrderDate
FROM SalesLT.Customer c
    CROSS APPLY dbo.GetCustomerOrders(c.CustomerID) o
    WHERE c.CustomerID = 29929;
```

![image.png](image%205.png)

![image.png](image%206.png)

![image.png](image%207.png)

## Fase 6: Crear el trigger de auditoría (`SalesLT.trg_LogOrderTotalChange`)

```sql
-- Audit table
IF OBJECT_ID('dbo.OrderAudit') IS NULL
BEGIN
		CREATE TABLE dbo.OrderAudit 
    (        
			   AuditID     INT IDENTITY(1,1) PRIMARY KEY,
			   OrderID     INT NOT NULL,
         OldTotal    DECIMAL(18,2) NULL,
         NewTotal    DECIMAL(18,2) NULL,
         ChangedAt   DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME()
    );
END
GO
-- Trigger on order details updates
CREATE OR ALTER TRIGGER SalesLT.trg_LogOrderTotalChange
ON SalesLT.SalesOrderDetail
AFTER INSERT, UPDATE 
AS
BEGIN
	SET NOCOUNT ON;    
		
		;WITH AffectedOrders AS (
							        SELECT SalesOrderID FROM inserted        
							        UNION        
							        SELECT SalesOrderID FROM deleted
							        ),
		NewTotals AS (
			       SELECT d.SalesOrderID, SUM(d.OrderQty * d.UnitPrice) AS Total        
			       FROM SalesLT.SalesOrderDetail d        
			       INNER JOIN AffectedOrders a ON d.SalesOrderID = a.SalesOrderID        
			       GROUP BY d.SalesOrderID    
			       ),
		InsertedTotals AS (
				       SELECT SalesOrderID, SUM(OrderQty * UnitPrice) AS Total        
				       FROM inserted        
				       GROUP BY SalesOrderID    ),
		DeletedTotals AS (        
								SELECT SalesOrderID, SUM(OrderQty * UnitPrice) AS Total        
								FROM deleted        
								GROUP BY SalesOrderID    
								)    
								INSERT INTO dbo.OrderAudit (OrderID, OldTotal, NewTotal)    
								SELECT        
								n.SalesOrderID,        
								n.Total - ISNULL(i.Total, 0) + ISNULL(d.Total, 0) AS OldTotal,        
								n.Total AS NewTotal    
								FROM NewTotals n    
								LEFT JOIN InsertedTotals i ON n.SalesOrderID = i.SalesOrderID    
								LEFT JOIN DeletedTotals d ON n.SalesOrderID = d.SalesOrderID;
END;
```

**Prueba:**

```sql
UPDATE d
SET OrderQty = OrderQty + 1
FROM SalesLT.SalesOrderDetail d
WHERE d.SalesOrderID = (SELECT TOP 1 SalesOrderID FROM SalesLT.SalesOrderHeader ORDER BY SalesOrderID DESC);

SELECT TOP (5) *
FROM dbo.OrderAuditORDER BY AuditID DESC;
```

![image.png](image%208.png)

![image.png](image%209.png)

## Fase 7: Limpieza (Cleanup)

- Object Explorer → expandir **Databases**
- Clic derecho sobre **AdventureWorksLT2025** → **Delete**

![image.png](image%2010.png)

- En el diálogo, marcar **Close existing connections**
- Clic en **OK**

![image.png](image%2011.png)

---

# **Resumen de incidencias del laboratorio**

1. **Confusión AdventureWorks vs AdventureWorksLT:** se descargó por error la versión completa en lugar de la Lightweight. Resuelto descargando el `.bak` correcto (con "LT" en el nombre) y restaurándolo como base separada.
2. **Base de datos no seleccionada en consultas nuevas:** SSMS abre ventanas nuevas en la última base usada (o `master`), no necesariamente en la deseada. Solución: usar siempre `USE AdventureWorksLT2025;` al inicio del script, o seleccionar la base en el desplegable de la barra de herramientas.